#!/usr/bin/expect -f

set timeout 20
set ip "193.203.160.3"
set user "s3t_dev"
set password "Travels2026#"

spawn ssh -o StrictHostKeyChecking=no $user@$ip

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "$ " {
        send "curl -v http://localhost:5000/api/auth/login\r"
        expect "$ "
        send "curl -v https://srisaisenthiltravels.cloud/api/\r"
        expect "$ "
        send "exit\r"
    }
}

expect eof
